/*
#include <iostream>

using namespace std;

// f(x)=x^2-3x+5

float f(float x) {
  float valor;
  valor = x * x - 3 * x + 5; // X ao quadrado é x vezes x
  return valor;
  // Aqui a função já está pronta
}

int main() {
  float a; // vai receber o valor que o usuario digitar
  cout << "Digite um número real\n";
  cin >> a;
  float valorf = f(a);
  cout << "O valor da função no ponto " << a << " é igual a: " << valorf
       << endl;

  return 0;
}
/
*/